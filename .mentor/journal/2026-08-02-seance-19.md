# Séance 19 — 2026-08-02 — STRUCTURER AVEC DES FONCTIONS (2)

## Objectif de la séance
2e extraction de fonction : une fonction qui renvoie une chaîne (`formater_machine`).

## Révision éclair
- `est_pair(x)` : `def est_pair(x): return x % 2 == 0` → **juste** (n'a pas écrit la partie usage `if est_pair(...)`, rappelée).
- Placement des `import` : au début du fichier → **juste**.

## Ce qui a été vu
- Une fonction peut renvoyer **n'importe quoi**, dont une **chaîne** construite (pas seulement un bool).
- Extraction de la mise en forme dans `def formater_machine(morceau): return "Machine : " + ... + "\n"`.

## Exercice fait — refactor `python\13_audit_reseau.py`
Ajout de `formater_machine(morceau)` ; la boucle utilise maintenant **deux** fonctions (`est_sur_reseau` + `formater_machine`). **Marche à l'identique**, boucle très lisible.

## Difficultés / observations
- **2e extraction réussie du 1er coup.**
- **A appliqué SEUL le retour de la séance 18** : `import datetime` remis tout en haut, avant les fonctions. → le feedback s'ancre, très bon signe.
- Code désormais bien structuré (imports → fonctions → code principal). L'architecture, son ancien point faible, progresse nettement.

## Concepts — bilan
- Structurer avec des fonctions (renvoyer un bool ET une chaîne, boucle lisible) → **se solidifie**. Applique les conventions.

## Prochaine étape prévue
**Dernière étape de structuration** : envelopper la logique principale dans une fonction (`def auditer(reseau):` ou `def main():`) et découvrir la convention **`if __name__ == "__main__":`** — la façon « pro » d'organiser un script Python. Ensuite : `pathlib`, puis SQL. Drills : usage d'une fonction dans un `if`, nommage.
