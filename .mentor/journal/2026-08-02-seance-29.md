# Séance 29 — 2026-08-02 — LE PONT SQL ↔ PYTHON (`sqlite3`) — lecture

## Objectif
Exécuter du SQL depuis Python avec le module `sqlite3` (lecture).

## Révision éclair
- Cold GROUP BY : encore rouillé (`SELECT * FROM machine COUNT`) → redonné le schéma + **astuce mémoire** : « compter PAR X » → la colonne X apparaît 2 fois (SELECT + GROUP BY).
- JOIN : idée globalement juste (combine plusieurs tables) → nuancé (relie via une clé commune ; ≠ GROUP BY).

## Ce qui a été vu
- `import sqlite3` ; schéma : `connect(fichier)` → `cursor()` → `execute("SQL")` → `fetchall()` (parcourir) → `close()`.
- Chaque ligne renvoyée = un **tuple** `(id, nom, ip, reseau)`.
- **Fermer DB Browser** quand on lance un script Python (SQLite = un seul écrivain, verrou → « database is locked »).

## Exercice fait — `python\bdd.py`
Connexion à `sql/machines.db`, `SELECT * FROM machines`, affichage de chaque ligne, `close()`. **Correct.**

## Difficultés / observations
- **Excellent réflexe** : a **renommé les variables** (`link`, `ross`) pour vérifier sa compréhension → a bien saisi que ce sont des objets (connexion/curseur), noms libres. Maturité d'apprentissage.
- Pont SQL→Python établi (1re exécution de SQL depuis du code).
- Production SQL à froid : toujours à driller (GROUP BY).

## Concepts — bilan
- `sqlite3` lecture (`connect`/`cursor`/`execute`/`fetchall`/`close`) → **vu, réussi**. À recroiser.

## Prochaine étape prévue
Suite du pont : **accéder aux colonnes** d'une ligne (`ligne[1]` = nom, indexation de tuple qu'il connaît), une **requête avec filtre** depuis Python, puis **ÉCRIRE dans la base** depuis Python (`INSERT` + `conn.commit()`). Drills : GROUP BY à froid.
