# Séance 20 — 2026-08-02 — STRUCTURER (3) : fonction principale + point d'entrée

## Objectif de la séance
Emballer la logique principale dans `auditer(reseau)` + convention `if __name__ == "__main__":`.

## Révision éclair
- `return` peut renvoyer quels types : **juste** (chaîne, entier, float, bool… toute valeur).
- Ordre d'exécution `sortie.write(formater_machine(morceau))` : **juste** (fonction interne d'abord).

## Ce qui a été vu
- Envelopper le code principal dans `def auditer(reseau):` (indentation d'un cran).
- Convention **`if __name__ == "__main__":`** : `__name__` vaut `"__main__"` quand on lance le fichier directement, le nom du module quand il est importé → sépare « outils réutilisables » de « code de lancement ».

## Exercice fait — refactor final `python\13_audit_reseau.py`
Structure complète : imports → `est_sur_reseau` → `formater_machine` → `auditer(reseau)` → `if __name__ == "__main__": input + auditer(reseau)`. **Réussi, marche à l'identique.** Bonne gestion de l'indentation du gros bloc déplacé.

## Difficultés / observations
- **Refactor complet réussi** (déplacement + indentation du bloc principal maîtrisés).
- **Excellente question de compréhension** : « pourquoi mettre le principal dans une fonction, et que veut dire `if __name__ == "__main__"` ? » → expliqué : réutilisation (`auditer("x")` plusieurs fois), importabilité (import sans déclencher l'input grâce au garde-fou), lisibilité. Honnêteté : gain modeste sur un mini-script, mais habitude qui passe à l'échelle.
- Continue de chercher le POURQUOI, pas juste le COMMENT — très bonne maturité.

## Concepts — bilan
- **Structuration d'un programme : ACQUISE dans les grandes lignes** (fonctions utilitaires + fonction principale + point d'entrée). L'ancien point faible « architecture » est devenu solide. 🎉
- `if __name__ == "__main__":` : compris dans le principe (à recroiser, concept abstrait).

## Prochaine étape prévue
Point d'inflexion : le module « Python pour l'infra » (fichiers, parsing, structuration) est bien couvert. Proposer à Ruben le prochain grand bloc :
- **(A) Démarrer SQL** (SQLite) — nouveau domaine, très demandé, complète le profil data/infra.
- **(B) Compléments Python** : `pathlib` (chemins modernes), `pip`/modules externes, un peu de POO.
Reco : lui laisser le choix. Drills : `if __name__ == "__main__"` (recroiser), nommage.
