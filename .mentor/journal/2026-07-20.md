# Séance 2 — 2026-07-20

## Objectif de la séance
Démarrer Python : premier script, `print`, variables et types de base.

## Révision éclair (git / terminal)
- 3 zones de git : a cité les **commandes** (`add`/`commit`/`push`) mais pas les **noms des zones** → à recroiser.
- Dossiers vides invisibles / `.gitkeep` : **oublié**.
- Rôle de `-u` dans `git push -u origin main` : **oublié**.
→ Ces 3 points restent à réviser (normal, une seule exposition). Le concept des commandes add/commit/push, lui, est bien retenu.

## Ce qui a été vu
- Vérif de Python (`python --version` → 3.13). Déjà installé.
- Notion de script `.py` et exécution (`python python\bonjour.py`).
- `print(...)` : fonction d'affichage ; texte = chaîne de caractères entre guillemets.
- Style PEP 8 : pas d'espace entre `print` et `(` — a compris que ça ne change pas le résultat (pure lisibilité).
- **Variables** : boîte étiquetée, affectation avec `=`.
- **Types de base** : `str`, `int`, `float`, `bool` ; `type(...)` pour vérifier.
- Découverte spontanée de `print(valeur, "texte")` (plusieurs arguments séparés par virgule) — initiative non demandée, très bien.

## Exercices faits
- `bonjour.py` : 3 `print` (carte d'identité machine). Correct du premier coup.
- `machine.py` (ou même fichier) : 4 variables de types différents + `type()`.

## Difficultés / observations
- **Piège type important** : a d'abord écrit `ram = "2"` et `freq = "3.5"` (avec guillemets) → valeurs devenues `str` au lieu de `int`/`float`. A trouvé la cause **lui-même** via `print(type(...))` puis corrigé. Concept guillemets=texte : bien ancré maintenant, mais à recroiser une fois pour confirmer.
- Petite confusion de vocabulaire « variable » vs « valeur/contenu » — clarifiée (la variable = la boîte ; le type = ce qu'il y a dedans).
- Toujours l'espace `print (` sur la 1re ligne par habitude — cosmétique, rappelé.

## Prochaine étape prévue
Python : les **opérateurs** (arithmétiques surtout, pour montrer la différence concrète nombre vs texte : `2 + 2` vs `"2" + "2"`), puis `input()` pour lire une saisie, et enchaîner sur les **conditions** (`if`). Réviser au passage : noms des 3 zones git + `.gitkeep` + `-u`.
